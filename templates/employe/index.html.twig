{% extends 'base.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Ici je listerai tout mes employés ✅</h1>
    <table class="table">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">nom</th>
            <th scope="col">prénom</th>
            <th scope="col">option</th>
        </tr>

        </thead>
        <tbody>
            {% for emp in employes %}
                <tr>
                    <th scope="row">{{emp.id}}</th>
                    <td>{{emp.lastname}} {{emp.firstname}}</td>
                    {# <td><img src="{{asset('./img/'~ emp.image ~'.jpg')}}"></td> #}
                    <td>
                        <a href="{{ path('detail_employe', {'employe':emp.id}) }}">detail</a>
                        <a href="{{ path('delete_employe', {'employe':emp.id}) }}">suprimer</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'D:/cours/symphony/starWars/src/Controller/PlanetesController.php'|file_link(0) }}">src/Controller/PlanetesController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'D:/cours/symphony/starWars/templates/planetes/index.html.twig'|file_link(0) }}">templates/planetes/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
